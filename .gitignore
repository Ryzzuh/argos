.venv/
__pycache__/

# data/ is entirely scratch: token cache, seat-layout and schedule caches, the
# local SQLite history and logs. None of it should travel with the repo.
data/

# state/ is intentionally NOT ignored: it is the cloud runner's diff baseline.
